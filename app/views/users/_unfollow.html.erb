<%= form_for @user.passive_relationships.find_by(followed_id: @user.id),
  html: {method: :delete}, remote: true do |f| %>
  <%= f.submit t("views.follow.unfollow"), class: "btn btn_primary" %>
<% end %>
